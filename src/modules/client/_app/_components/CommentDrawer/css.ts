import { css } from "@pigment-css/react"

const skeletionContainer = css({
  padding:10
});

const skeletionTitle = css({
  marginBottom:5
})

const className =  {
  skeletionContainer,
  skeletionTitle
}

export default className;